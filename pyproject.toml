[project]
name = "flatsdev"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "Flask==3.1.3",
    "Flask-SQLAlchemy==3.1.1",
    "psycopg[binary]==3.3.3",
]

[project.scripts]
app = "index:app"
